# CLAUDE.md — 제주대 공과대학 교수진 안내 앱

이 파일은 이 저장소에서 작업하는 Claude를 위한 안내서입니다. 작업을 시작하기 전에 전체를 읽으십시오.

## 1. 이 프로젝트가 무엇인가

제주대학교 공과대학 12개 학과 70명 교수진을 정리한 단일 페이지 웹 앱입니다.

- 배포: https://yungbyun.github.io/jejunu-faculty/ (GitHub Pages, `main` 브랜치 루트)
- 로컬: `C:\Users\Yung\Dropbox\incomming\MyAI\Claude\학장선거\jejunu-faculty`

**중요한 맥락:** 사용자(변영철 교수, 컴퓨터공학전공)는 **공과대학 학장 선거**를 준비하고 있습니다.
앱의 "선호도"(확·긍·중·모·부·비)는 각 동료 교수가 자신을 지지할 가능성을 기록한 것입니다.
긍(긍정)은 2026-09-21 에 확과 중 사이로 넣었습니다. 단계를 늘리거나 줄이려면 `app.js` 의
`CONFIG.RATINGS.LABELS`·`NAMES`·`SCORE`·`RCOLOR`·`rClass` 와 `styles.css` 의 `rate__b--*`·
`rs--*`·`--rs-*`·`sw--*`·`sbar__seg--*`·`.prof[data-rating="…"]`, 그리고 **`apps-script/Code.gs`
의 `RATINGS`** 를 같이 고쳐야 합니다. 서버를 안 고치면 `bad rating` 으로 거절당해 시트에
안 올라갑니다(앱에는 남고 재배포 뒤 다시 올라갑니다).
즉 이 자료는 선거 준비용 개인 자료이며, **정확성이 분량보다 훨씬 중요합니다.**
사실을 모르면 추측해서 채우지 말고 비워 두고 솔직히 보고하십시오. 임용 연도를 박사학위 연도나
첫 논문 연도로 추정하는 식의 채움은 과거에 명시적으로 거부된 방식입니다.

## 2. 구조

```
index.html          버전 확인 후 app.js / styles.css 를 캐시버스팅해서 로드
app.js              라우팅·렌더링·로그인·선호도·AI 재작성 등 모든 로직 (약 110KB, 단일 파일)
styles.css          전체 스타일 (단일 파일)
version.json        {"v":"20260919t"} — 배포 때마다 값을 올린다
data/professors.json    교수 기본 데이터(시트 폴백)
data/insights/<dept>.json   학과별 상세 데이터 12개
data/photos/            사진 + manifest.json
apps-script/Code.gs     Google Apps Script 웹 앱 소스 (저장소에는 사본만, 실제 배포는 수동)
                        시트 탭: ratings / settings / airewrites / outbox(예약 발송)
a.bat                   git add -A / commit / push 한 번에
```

라우팅은 해시 기반입니다: `#/`, `#/dept/<id>`, `#/dept/<id>/prof/<slug>`, `#/stats`, `#/letters`, `#/outreach`, `#/quiz`.

학과 id: `foodse, chemeng, archieng, elec, telecom, comdol, ai, ce, mse, nuclear, archidesign, civil`

## 3. 데이터 흐름

1. 앱은 Google 스프레드시트(ID `1YhXtXKg0SufowUr28Vx3y2bwSA5hP2FHIYg1ZPeJu6U`)를 gviz CSV로 읽으려 시도합니다.
2. **현재 이 시트는 비공개이므로 401이 납니다.** 그래서 앱은 `data/professors.json` 폴백을 씁니다.
   화면 하단에 "시트를 읽지 못해 백업 데이터를 표시합니다"가 뜨는 것은 **정상이며 고장이 아닙니다.**
3. 교수별 상세 내용은 `data/insights/<dept>.json` 에서 읽습니다.

> **시트를 절대 공개로 바꾸지 마십시오.** 같은 스프레드시트의 `ratings` 탭에 동료 교수 평가가 들어 있습니다.

### insights JSON 스키마

```jsonc
{
  "updated": "…",
  "profs": {
    "<slug>": {
      "highlights": [...],
      "items":  [...],            // 검색 결과 항목
      "note":   "...",
      "joined": {                 // 제주대 임용 시기 (검색 결과 맨 위에 표시)
        "year": 1986,
        "text": "1986년부터 제주대 재직",
        "src": "https://…professorinfo.htm",
        "src_name": "학과 홈페이지 교수 약력",
        "memo": "조교 시작"       // 정식 전임 임용이 아닌 경우에만
      },
      "ai": {                     // AI 융합 방향
        "sum": "...",
        "items": [ { "t": "제목", "term": "용어 풀이", "concl": "결론" } ]
      },
      "kw": ["키워드", ...]        // 강조할 키워드, 긴 것부터 정렬
    }
  }
}
```

## 4. 서버 (Apps Script)

`apps-script/Code.gs` 는 사본입니다. 실제 서버는 Apps Script 웹 앱이며 URL은 `app.js` 의
`CONFIG.RATINGS.API_URL` 에 있습니다. 액션: `session`, `list`, `setting`, `set`,
`airewrite`, `airewrite_reset`, 그리고 예약 발송용 `outbox` · `queue` · `unqueue`.

개인 설정은 `settings` 탭에 `email / key / value`로 저장되고, `setting` 액션은 **키 이름을 가리지 않습니다.**
그래서 새로운 개인 설정을 추가할 때 서버를 고칠 필요가 없습니다. 현재 쓰는 키는 `quiz-depts`, `quiz-ex`,
그리고 관심 교수 목록 `fav`(값은 `["<dept_id>/<slug>", ...]`)입니다. app.js 쪽은 `SET_KEYS`와
`settingValue` / `settingApplyLocal` / `SET_LOCAL_KEY` 네 군데에 분기를 넣으면 됩니다.

**고쳤을 때 배포 방법 (사용자가 직접 해야 함):** Apps Script 편집기에 붙여넣고 저장만 하면 아무 일도
일어나지 않습니다. 반드시 **배포 → 배포 관리 → 새 버전**으로 다시 배포해야 반영됩니다.
반영이 안 되면 앱에서 `bad action` 오류가 납니다.

### 절대 깨뜨리면 안 되는 제약

- **`apiPost` 는 헤더를 하나도 보내지 않습니다.** 이것이 요청을 CORS "simple request"로 유지시킵니다.
  `Content-Type: application/json` 등을 추가하면 preflight(OPTIONS)가 발생하고 Apps Script는
  이에 응답할 수 없어 **저장 기능 전체가 죽습니다.**
- Anthropic API 키(`ANTHROPIC_KEY`)는 Apps Script의 스크립트 속성에만 존재합니다.
  **Claude는 이 키를 절대 받거나 다루지 않습니다.** 대화나 파일에 노출되면 즉시 폐기해야 합니다.
  APP_SECRET(앱 토큰 HMAC 키)도 같습니다.

## 5. 배포 절차

1. 파일을 수정한다.
2. **화면을 고쳤으면 `tools/shots.mjs` 로 먼저 눈으로 확인한다** (§8 참고). 확인 전에는 푸시하지 않는다.
3. **`version.json` 의 값을 반드시 올린다** (`20260919t` → `20260919u` 식). 안 올리면 사용자
   브라우저가 옛 `app.js`/`styles.css`를 계속 씁니다.
4. `a.bat` 실행 또는 `git add -A && git commit -m "..." && git push`.
5. 확인은 캐시를 우회해서: `https://yungbyun.github.io/jejunu-faculty/version.json?cb=<아무값>`
   GitHub Pages 반영에는 보통 1~2분 걸립니다. 브라우저에서는 `?r=1` 을 붙이거나 `Ctrl+Shift+R`.

## 6. 글쓰기 규칙 (사용자가 직접 정한 것 — 반드시 지킬 것)

### AI 융합 방향 (`ai.items`)

전문용어를 **먼저 풀어서 설명하고**, 왜 그것이 어려운 문제인지 말한 다음, 마지막에 결론을 냅니다.
비전문가(다른 학과 교수, 직원)가 읽는다고 가정하고 실생활 비유를 씁니다.

- `term` — 용어 풀이 + 왜 어려운 문제인지 (접었다 펼 수 있는 부분)
- `concl` — 결론 한 문장 (항상 보이는 부분)

사용자가 직접 제시한 모범 예시의 뼈대: "CoFe·NiFe는 코발트-철, 니켈-철 합금계 촉매를 가리킵니다.
조성은 그 합금의 배합 비율입니다. … 따라서 금속 배합을 수백 가지 다 만들어 보는 대신, 지금까지 만들어 본
결과를 근거로 다음에 시도할 배합을 몇 개만 골라 줍니다."

화면 표시는 **번호 배지 + 결론 먼저** 방식입니다. (초록색 왼쪽 바 디자인은 거부되었습니다.)

### 강조

- 모델 출력의 `**굵게**` 는 항목당 **2~3곳까지만**. 그 외 마크다운 문법은 금지.
- 렌더링은 `aiRich()` 가 담당합니다: **HTML을 먼저 이스케이프한 뒤** 강조 표시를 `<strong>`으로 바꿉니다.
  순서를 뒤집으면 XSS가 열립니다.
- `kw` 키워드는 원문에 **글자 그대로 존재해야** 합니다. 합치기 전에 검증하십시오.

## 6-2. 최근에 추가된 기능 (2026-09-20~21)

**관심 교수 표시.** 카드의 체크 버튼으로 켜고 끕니다. 저장은 `settings` 탭의 `fav` 키
(값은 `["<dept_id>/<slug>", ...]`)이고 **고른 순서를 그대로 유지**합니다 — 정렬하지 마십시오.
`settingValue` 는 `fav` 만 정렬 없이 돌려주고, `applySettings` 의 비교도 `fav` 는 순서까지 따집니다.
관련 함수: `favs / isFav / favCount / toggleFav / favBtn / bindFavs / favSection`.
첫 화면 상단에 관심 교수 명단 패널(사진·이름·학과·직위·방 번호·선호도 칩·해제 ×)이 있고,
학과 행의 작은 사진에는 금색 테두리, 학과 목록에는 `✓ N` 칩이 붙습니다.

**교수 카드(모바일 D안).** 폰에서는 사진 120px(작은 화면 108px)이 왼쪽, 오른쪽에 정보입니다.
이름 줄은 `이름 · 직위 · 관심 버튼` 한 줄이고 영문명은 숨깁니다. 사진 위에는 아무것도 얹지 않습니다.
아래쪽은 `prof__bottom` 묶음(방 번호 줄 / 전화번호 줄 / 선호도·카운터)이며 카드 바닥에 붙어
모든 카드에서 카운터 위치가 같습니다. PC 는 사진이 위, 선호도·관심 버튼은 절대 위치입니다.

**연구실 칸 분리.** `officeParts()` 가 `"연구실 이름 (건물 호실)"` 을 갈라, 이름은 영문명 옆 칩으로,
위치만 방 번호 줄에 넣습니다. 방 번호는 문자열 끝에 오는 경우가 많으므로 **말줄임으로 자르지 말고**
줄을 바꾸십시오.

**분석 페이지.** 반원 게이지 하나(`donut()` — 이름은 도넛이지만 반원입니다)와 학과별 분포 막대,
선호도별 교수 목록만 남았습니다. 제목·설명·요약 카드·타일·키워드 분석·직급별/학과별 표는 모두
제거했습니다. 학과별 막대는 **가장 인원이 많은 학과를 100%로 잡고 인원 수에 비례해 길이를 줄입니다**
(`stackBar(profs, {max})`). `tagStats / sureRate / avgScore / fmt1 / fmtPct` 는 지금 호출되는 곳이
없지만 되살릴 수 있게 남겨 두었습니다.

**퀴즈 — 이름 네모 칸 삭제, 앞뒤 이동 (2026-09-20).** 사진 밑에 이름 글자 수만큼 놓여 있던
네모 칸(`.qz-mask` / `.qz-ch`)을 없앴습니다. **이름 입력칸(`#qzIn`)은 그대로 둡니다** — 타이핑으로
맞히는 것이 이 퀴즈의 기본 방식입니다. (한 번 입력칸을 지웠다가 되돌린 적이 있으니 주의.)
힌트로 열린 글자는 힌트 줄에 `학과 · 김○○` 처럼 글로 보여 줍니다(`quizLetters`).
힌트 1은 학과, 그다음부터 한 글자씩이며 아직 안 열린 자리는 `○` 입니다.
`← 이전` 버튼과 사진을 오른쪽으로 미는 동작으로 앞 문제로 돌아갑니다.
문제별 상태(`hints/wrong/state/gained/heard`)는 `quiz.res[i]` 에 넣었다 꺼내므로 되돌아가도
점수가 두 번 오르지 않고, 건너뛴 문제는 다시 풀 수 있습니다.
관련 함수: `quizSnap / quizGo / quizNext / quizPrev / quizStateAt / quizLetters`.
푼 문제 수는 `quiz.i` 가 아니라 `quizStateAt` 으로 셉니다(앞뒤로 오가도 맞아야 하므로).
폰에서 `.qz-acts` 는 `repeat(2,1fr)` 2x2 입니다 — `auto-fit` 으로 두면 네 개가 3+1 로 갈라집니다.

**올해 부임한 교수에 병아리 (2026-09-22).** `professors.json` 의 `joined_year` 가 `app.js` 의
`NEW_YEAR` 와 같으면 사진 오른쪽 아래에 병아리를 답니다(`chickBadge`, `.newp`). 해가 바뀌면
`NEW_YEAR` 를 손으로 올리십시오 — 그 해 부임자를 다 확인한 뒤에 켜야 빠진 사람이 없습니다.

지금 표시된 6명과 근거:

| 교수 | 근거 |
|---|---|
| 식품 박종진 · 토목 박정하 | insights 의 "2026년 9월 임용" |
| 기계 김민철 | insights 의 "2026년 3월 임용" |
| 컴퓨터 박승민 · 통신 임형용 | insights 의 "2026년 임용" |
| 원자력 최찬수 | 2026-03-05 공대 명단에 없고, 학과 홈페이지 경력이 2026.08 까지 플로리다대 박사후연구원 |
| 원자력 이명규 · 통신 유대성 | **본인(변영철 교수) 확인** (2026-09-22). 홈페이지에 임용 연도가 없어 앞의 방법으로는 못 잡던 사람들입니다 |

임용 시기가 안 잡힌 교수가 아직 37명입니다. 더 나오면 `joined_year` 만 넣으면 바로 붙습니다.

**상세 드로어 — 학과명을 누르면 그 학과로 (2026-09-22).** 머리의 `dept-pill` 을 `<a href="#/dept/…">`
로 바꿨습니다. 드로어는 늘 학과 화면 위에 뜨므로 주소만 바꾸면 `render()` 가 알아서 닫고
학과 화면을 보여 줍니다. 닫을 때 `flushMemo()` 가 돌아 쓰다 만 메모도 저장됩니다.

**메모 — 굵게 (2026-09-22).** 메모 칸에서 글자를 고르고 칸 **아래** `가` 단추나 `Ctrl+B` 를 누르면
`**이렇게**` 표시가 붙고, 칸 아래 미리보기와 접촉 화면에서 굵게 보입니다(`memoHtml`).
**저장은 평문 그대로입니다** — 시트의 memo 열과 CSV 를 사람이 읽어야 하고, 그래야 서버를
고칠 일도 없습니다. 미리보기는 `**` 가 들어 있을 때만 뜹니다. 칸 자체를 서식 편집기로
만들지 않은 것은 한글 입력기와 붙여넣기에서 커서가 자주 튀기 때문입니다.
테스트는 `tools/t-memo.mjs` (16개).

**분석 — 도넛 아래 확 / 확+긍 두 숫자 (2026-09-21).** 도넛 가운데에 확 하나만 크게 띄우던 것을
빼고, 아래에 `확(확실)` 과 `확+긍(지지 기반)` 을 같은 크기로 나란히 놓았습니다(`.pie__kpi`).
가운데에 큰 숫자 둘을 넣으면 읽히지 않아 바깥으로 뺐고, 도넛 viewBox 높이를 138→126 으로
줄여 빈자리를 없앴습니다. 분모는 둘 다 평가제외(비)를 뺀 인원입니다.

**분석 — 선호도별 교수 목록을 가로로, 이름만 (2026-09-20).** 세로 4열로 쌓던 것을 선호도마다
한 줄씩 가로로 흘리도록 바꾸고, 칩에서 **학과·직위를 뺐습니다**. 세로로 두면 선호도마다 사람 수만큼
길어졌기 때문이며, PC 기준 페이지 높이가 2311px → 1527px → 1343px 로 줄었습니다.
학과·직위는 `title` 속성으로만 남아 마우스를 올리면 보입니다. 칩 전체가 상세 페이지 링크입니다.
폰에서는 `.st-list` 가 한 열이 되어 선호도 이름이 줄 위로 올라갑니다.

**접촉 화면 (2026-09-20).** 상단 탭에 `접촉`(`#/outreach`)을 더했습니다. 학장 선거 지지 요청
메시지를 만들고, 누구에게 어디까지 갔는지 관리하는 화면입니다.

저장은 `settings` 탭의 새 키 두 개입니다. **서버(Code.gs)는 고치지 않았습니다** — `setting` 액션이
키 이름을 가리지 않기 때문입니다.

```jsonc
mailcnt  {"<dept_id>/<slug>": 2}        // 메일 보낸 횟수
```

**성향 태그(연·강·둘)도 뺐습니다(2026-09-21).** 회차가 들어오면서 개인화는 갈래(A~E)가 맡게 되어
`epFinal()` 은 성향을 보지 않습니다. 성향을 쓰던 옛 조립 함수 `draftBody` 는 1회차가 항상 있는 한
불릴 일이 없어 같이 지웠습니다. 분류 체계가 둘이면 어느 것으로 판단할지 매번 헷갈립니다.
강의 쪽 교수께 다르게 쓸 일이 생기면 **학과 원고나 교수별 원고**로 하십시오 — 더 유연하고 미리
태깅할 필요가 없습니다. `trait` · `SET_TR` 은 지워졌으니 되살리지 마십시오.

**접촉 상태(미접촉·초안·보냄·답장)와 선호도 칩은 이 화면에서 뺐습니다(2026-09-20).**
회차가 들어오면서 "누구에게 어디까지 갔는가"를 회차 발송 기록(`epsent`)이 대신하게 되어,
상태를 따로 두면 같은 것을 두 군데서 관리하게 됩니다. 머리말도 회차 발송 기준으로 바뀌었습니다.
`contact` 키와 `SET_CT` 는 지워졌으니 되살리지 마십시오. 선호도는 학과·분석 화면에 그대로 있습니다.

**메일 보낸 횟수는 학과 화면의 방문 카운터와 다른 자리에 저장됩니다.** 방문은 `ratings` 탭의 `met`
열이고(서버가 아는 필드), 메일 횟수는 `settings` 탭의 `mailcnt` 키입니다 — 서버를 고치지 않으려고
이렇게 했습니다. 그래서 카운터 DOM 도 `data-key` 가 아니라 `data-mk` 를 씁니다.
`refreshRatingUI` 가 `.counter[data-key=...]` 를 훑어 숫자를 덮어쓰기 때문에, 같은 속성을 쓰면
메일 횟수가 방문 횟수로 바뀝니다. **새 카운터를 또 만들 때도 `data-key` 는 피하십시오.**
'메일 앱에서 열기' 를 누르면 1 늘어나고, 잘못 세면 − 로 줄이면 됩니다.

**이 두 키는 값이 배열이 아니라 객체입니다.** `applySettings` 가 `Array.isArray` 만 보고 거르던 것을
`SET_OBJ` 목록으로 갈라 객체도 받도록 고쳤습니다. 앞으로 배열이 아닌 설정 키를 더할 때는 반드시
`SET_OBJ` 에 넣으십시오. 넣지 않으면 다른 기기에서 바꾼 값이 조용히 무시됩니다.
같은 이유로 `applySettings` 의 `changed` 는 불리언이 아니라 바뀐 키 목록이며, 퀴즈 키가 바뀐 때만
`quizStart()` 를 부릅니다(예전에는 관계없는 설정이 바뀌어도 퀴즈가 새로 시작됐습니다).

초안은 `draftBody(p, 채널, one)` 이 조립합니다. `one` 은 그 교수 `data/insights` 의
`ai.items[0].concl` 한 줄이고, 없으면 부트캠프 이야기로 대체합니다. 채널은 메일·문자·카톡이며
insights 는 행을 펼칠 때 그 학과 것만 읽어 옵니다(`loadInsights`). 보내는 사람 정보는 `ME` 상수입니다.

메시지 내용의 축은 면담에서 확인된 것입니다 — **"AI 가 중심이 아니라 각 학과가 중심이고 AI 는 거기
붙어서 돕는다"**. 최희복 교수가 강하게 동의한 지점이라 도메인 학과 교수들에게 공통으로 씁니다.
성향이 `강` 이면 이 문장이 수업 쪽으로 바뀌고, 부트캠프 학점을 교양으로 열겠다는 항목이 붙습니다
(전공으로 넣으면 학과 전공 과목이 하나 날아간다는 지적을 반영).

**이 앱은 메시지를 보내지 않습니다.** 복사와 `mailto:` 까지만 합니다. 예약 발송은 Apps Script 에
`outbox` 탭과 시간 트리거를 붙여야 하고, 그때는 서버를 고쳐 **직접 재배포**해야 합니다(아직 안 했음).
카카오톡은 개인 계정 자동 발송 API 가 없어 복사해서 직접 보내는 수밖에 없습니다.

최희복 교수는 `professors.json` 에 이메일이 비어 있어 목록에 "메일 없음"으로 표시되고
`mailto:` 버튼 대신 안내가 나옵니다. 70명 중 이 한 명뿐입니다.

확인 스크립트(로컬 서버가 떠 있어야 합니다 — `python -m http.server 8080 --bind 127.0.0.1`):

```
cd tools
node t-outreach.mjs     접촉 화면 동작 17가지
node t-console.mjs      모든 화면을 돌며 콘솔 오류 확인
```

`t-console.mjs` 가 남기는 `GSI_LOGGER ... origin is not allowed` 와 `Provider's accounts list is
empty` 는 localhost 가 구글 OAuth 승인 출처가 아니라서 나는 것으로, 정상입니다.

**예약 발송 (2026-09-20).** 접촉 화면에서 메일을 예약해 두면 Apps Script 의 시간 트리거가 보냅니다.
메일은 스크립트를 승인한 계정(= 본인 Gmail)에서 나갑니다. **앱은 예약만 쌓고 보내지 않습니다.**

시트에 `outbox` 탭이 생깁니다 — `id, email, to, key, subject, body, send_at, status, sent_at, error`.
`status` 는 `queued / sent / error / canceled` 이고, 같은 사람에게 대기 중인 예약이 이미 있으면
그 줄을 갈아 끼웁니다(중복 발송 방지). 취소는 아직 안 나간 것만 됩니다.

서버 액션 세 개가 늘었습니다: `outbox`(목록·상태·남은 할당량), `queue`(예약), `unqueue`(취소).

### 안전장치 — 이 순서를 지키십시오

`sendDue()` 는 스크립트 속성 `OUTBOX` 가 `'on'` 일 때만 움직입니다. **트리거만 만들어 두어도 메일은
한 통도 나가지 않습니다.** Apps Script 편집기에서 함수를 골라 직접 실행합니다.

```
1) installOutbox()    5분마다 도는 트리거를 만든다 (아직 안 보냄)
2) outboxDryRun()     연습 모드 — 보낸 것으로 표시만 하고 실제로는 안 보낸다. 먼저 이걸로 확인할 것
3) outboxStart()      진짜로 보내기 시작
   outboxStop()       언제든 멈춤. 대기열은 그대로 남는다
   outboxStatus()     지금 상태와 남은 하루 할당량을 로그로 본다
```

한 번 돌 때 최대 `OUT_MAX_PER_RUN`(8)통만 보내고, 남은 하루 할당량이 `OUT_MIN_QUOTA`(10)보다
적으면 그날은 멈춥니다. 일반 Gmail 계정은 하루 100통이라 70명은 한 번에 됩니다만, 한꺼번에 나가면
단체메일로 보이므로 시각을 나눠 거는 편이 낫습니다.

**이 기능을 쓰려면 Apps Script 를 반드시 다시 배포해야 합니다**(배포 → 배포 관리 → 새 버전).
재배포 전에는 앱이 `bad action` 을 받고 접촉 화면에 재배포 안내가 뜹니다.

### 시각 처리

브라우저는 `datetime-local` 로 받은 지역 시각을 **UTC ISO 문자열**로 바꿔 보내고(`sendAt`),
서버는 `new Date(sendAt).getTime() <= now` 로만 비교합니다. Apps Script 프로젝트 표준시와
무관하게 맞습니다. 기본값은 내일 오전 9시입니다.

### 안 되는 것

카카오톡은 개인 계정 자동 발송 API 가 없습니다(알림톡은 사업자 등록·템플릿 심사가 필요하고 선거
홍보는 심사에서 막힐 가능성이 큽니다). 문자도 자동 발송하려면 유료 문자 API 계약이 필요합니다.
둘 다 복사해서 직접 보내는 방식으로 남겨 두었습니다.

확인 스크립트: `tools/t-sched.mjs` — `ratingsApi` 를 가짜로 바꿔 끼워 서버를 부르지 않고
재배포 안내·꺼짐 경고·예약·취소·채널별 노출을 확인합니다(16가지).

**회차 (2026-09-20).** 지지 요청 메일은 한 번이 아니라 **12월 투표일까지 열 번 넘게** 나눠 보냅니다.
그래서 접촉 화면에 회차 개념을 넣었습니다. 회차 하나 = **공통 본문 + 갈래별 한 줄**입니다.

`settings` 탭 키 두 개를 더 씁니다(서버 수정 없음).

```jsonc
eps     {"1": {subject, body, sms, seg:{A..E}, dept:{<dept_id>:"..."}}}
epsent  {"<dept_id>/<slug>": [1, 2]}          // 이 사람에게 나간 회차
```

### 갈래는 학과로 자동으로 갈립니다 (손으로 태깅하지 않음)

2026-09 부트캠프 명단을 근거로 `SEG` 에 박아 두었습니다. 명단이 바뀌면 여기를 고칩니다.

| 갈래 | 학과 | 인원 | 왜 |
|---|---|---|---|
| A 전자·통신 | comdol, telecom | 11 | 캡스톤디자인 과목으로 30명·10명이 이미 참여 |
| B 참여 학과 | mse, nuclear, foodse, civil, elec | 31 | 부트캠프에 학생이 왔음 |
| C 미참여 학과 | chemeng, archidesign, archieng | 17 | 한 명도 없었음 |
| D 컴퓨터공학과 | ce | 6 | "컴송합니다"를 밖에 꺼내는 일은 우리가 한다 |
| E 인공지능학과 | ai | 5 | **역할이 커진다** 쪽으로 |

**E 갈래를 따로 둔 이유가 중요합니다.** 1회차의 "컴송합니다"(컴퓨터공학과라서 죄송합니다)가
"인송합니다"로 번지면 인공지능학과 교수님들께는 자기 학과가 무의미해졌다는 말로 읽힙니다.
실제로는 정반대여야 합니다 — 각 학과가 중심이고 AI가 붙어 돕는 구조라면 붙어 줄 사람이
필요한 곳이 학과 하나에서 공대 열두 개로 늘어납니다. **E 갈래 문장을 고칠 때 이 방향을 깨지 마십시오.**
같은 이유로 D(컴공 동료)는 "우리가 당했다"가 아니라 "우리가 나서야 한다"로 씁니다.

### 자리표시자

회차 원고는 채널마다 따로 씁니다 — `body`(메일) · `sms`(문자) · `kakao`(카톡).
`kakao` 가 비어 있으면 `sms` 를 씁니다. **문자는 짧게 유지하십시오** — 1회차 기준 문자 333자,
카톡 445자, 메일 954자이고, 문자에서는 사업단 소개 문단을 뺐습니다.

본문과 갈래 문장 안에서 `{이름}` `{학과}` `{개인화}` `{연구}` 가 사람마다 치환됩니다.
`{개인화}` 자리에 갈래 문장이, `{연구}` 자리에 그 교수 `data/insights` 의 `ai.items[0].concl`
한 줄이 들어갑니다. 학과별 문장(`dept`)이 있으면 갈래 문장보다 먼저 씁니다 — 전자 30명·통신 10명처럼
숫자가 다를 때 씁니다.

### 1회차 내용은 `EP1` 상수에 박혀 있습니다

앱에서 고치면 `settings` 에 저장되어 그쪽이 이깁니다. `EP1` 은 처음 한 번 채우는 기본값입니다.

### 전체 → 학과 → 교수 3단 원고 (2026-09-20)

회차 본문이 바탕이고, **학과나 교수 단계에 글이 있으면 그것이 이깁니다. 비우면 위 단계를 그대로 씁니다.**
그래서 할 말이 다른 곳만 채우면 되고, 나머지는 손대지 않아도 됩니다.

```jsonc
epov<N>  {                                  // 회차마다 키가 따로다
  "dept": {"comdol": {body, sms, kakao}},   // 그 학과용 전체 원고
  "prof": {"comdol/ko-seok-jun": {body, sms, kakao}}
}
```

**회차마다 키를 따로 쓰는 이유가 중요합니다.** 시트 칸 하나가 5만 자인데, 학과 12개 × 본문 1,000자 ×
10회차면 12만 자로 넘칩니다. 그래서 `SET_KEYS` 는 고정 배열이 아니라 `setKeys()` 가 회차 수만큼
`epov1`, `epov2` … 를 붙여 돌려줍니다. **설정 키를 더할 때 `SET_KEYS` 만 보고 고치면 안 됩니다** —
`setKeys()` · `settingValue` · `settingApplyLocal` · `setLocalKey` · `isEpov` 를 함께 보십시오.

조립 순서는 `epRaw()` 에 있습니다: 교수 → 학과 → 회차. 어느 단계가 쓰이는지는 `epLevel()` 이 알려 주고
초안 패널에 "지금 쓰는 원고: 학과 단계"로 표시됩니다. 채널(메일·문자·카톡)마다 따로 덮어쓸 수 있습니다.

접촉 화면에서 **학과를 고른 뒤 `원고 고치기`** 를 누르면 그 학과 원고가, **`전체`일 때 누르면** 회차 전체
원고가 열립니다.

### AI 다시 쓰기

학과 단계와 교수 단계에 `AI로 다시 쓰기` 버튼이 있습니다. 서버의 `rewrite` 액션이 Claude를 부르고,
`airThrottle` 로 연타를 막습니다(3초). **이 기능은 Apps Script 재배포가 필요합니다.**

`mailRewrite()` 의 시스템 프롬프트가 지키는 것: 사실·숫자를 바꾸지 않기, 없는 공약을 만들지 않기,
문단 수와 길이 유지, 과장 금지. 그리고 **자리표시자(`{이름}` 등)가 빠지면 결과를 버리고 오류를 돌려줍니다** —
빠진 채로 저장되면 조립이 깨지기 때문입니다.

**AI로 12개 학과를 모두 다시 쓰지 마십시오.** 학과마다 문체가 갈라지면 복도에서 메일을 견줘 본 교수들에게
"AI로 돌렸구나"가 드러납니다. 본문은 하나로 단단히 두고 할 말이 다른 학과만 손대는 편이 낫습니다.

확인 스크립트: `tools/t-tier.mjs` (3단 상속·되돌리기·회차 분리·AI 호출 21가지).

### 원고 화면 (2026-09-21)

상단 메뉴에 `원고`(`#/letters`)를 더했습니다. 역할을 이렇게 가릅니다.

- **원고 탭** — 회차를 쓰고 관리 (전체 원고 + 학과 원고)
- **접촉 탭** — 사람을 보고 보내기 (교수별 손질 + 발송·예약)

회차 목록에 이름·상태·예정일·진행이 한 줄씩 나오고, 아래가 편집기입니다.
**왼쪽에서 고치면 오른쪽 미리보기가 바로 바뀝니다.** 갈래 A~E 버튼으로 미리볼 사람을 바꿉니다.
대상 드롭다운에서 학과를 고르면 그 학과 원고를 고칩니다.

**입력 중에는 화면을 다시 그리지 않습니다.** 다시 그리면 커서가 날아가기 때문입니다 —
`letTouch()` 가 0.7초 뒤에 저장만 하고, 미리보기는 `textContent` 만 갈아 끼웁니다.
**이 화면에 무엇을 더할 때 `renderLetters()` 를 입력 이벤트에서 부르지 마십시오.**

회차에 `name`(짧은 이름)과 `planAt`(예정일)이 생겼습니다. 상태는 저장하지 않고
`epStatus()` 가 본문과 발송 기록으로 계산합니다. 회차 10개의 이름은 `EP_NAMES` 에 있고,
1·2회차 내용은 `EP1` · `EP2` 에 들어 있습니다(앱에서 고치면 그쪽이 이깁니다).

### 핸드폰 번호 — 공개 파일에 넣지 마십시오

**이 저장소는 공개이고 `data/professors.json` 은 로그인 없이 내려받힙니다(확인함).**
그래서 개인 휴대폰 번호는 거기 절대 넣지 않고, 선호도·메모와 같은 길로
**비공개 시트(`settings` 탭의 `mobile` 키)** 에만 둡니다. 로그인한 본인에게만 보입니다.

```jsonc
mobile  {"<dept_id>/<slug>": "010-0000-0000"}
```

- 교수 카드의 연구실 전화 바로 아래에 표시(`.po__mob`)
- **상세 드로어의 핸드폰 칸은 2026-09-22 에 뺐습니다.** 카드에서 이미 보이므로 자리만 차지했습니다.
  넣고 고치고 지우는 것은 접촉 화면의 가져오기 한 곳으로 모았습니다 — 한 사람만 고칠 때도
  `{"통신공학과/유대성": "010-0000-0000"}` 처럼 붙여넣으면 되고, **번호를 비우면 지웁니다.**
- 접촉 화면 맨 아래 `핸드폰 번호 한 번에 가져오기` 로 JSON 을 붙여넣어 채웁니다(`mobileImport`)
- 키는 `"학과이름/교수이름"`(권장) · `"dept_id/slug"` · 이름만, 셋 다 받습니다. 동명이인은
  학과를 붙여야 받습니다. slug 만 받던 때에는 왜 버려졌는지 화면에 안 보여 원인을 못 찾았습니다.
  지금은 `N개 중 M명 반영 / 명단에 없는 이름 …` 을 상자 안에 남겨 둡니다.
- 원본은 2026-03-05 기준 공과대학 교직원 명단 스캔이고, 대조 결과는 저장소 **바깥**
  `학장선거/핸드폰-가져오기.json` 에 두었습니다. 65명 확보, 5명(박종진·김상재·고진환·최찬수·박정하)은
  스캔에 이름이 없어 직접 넣어야 합니다.

**이름 대조에 퍼지 매칭을 쓸 때 조심하십시오.** 한 글자 차이로 맞추면 `박종진 ← 박은진`,
`김상재 ← 김상진` 처럼 **실재하는 다른 교수**를 끌어옵니다. 엉뚱한 분께 선거 문자가 갑니다.
지금 규칙은 (1) 정확히 같은 이름 먼저, (2) 남은 사람만 한 글자 차이를 보되 그 줄 이름이 다른 교수의
정확한 이름이면 건너뛰고, (3) 이미 쓰인 번호면 건너뜁니다. 번호가 전부 고유한지로 검산하십시오.

확인 스크립트: `tools/t-mobile.mjs` — 맨 처음 검사가 **공개 `professors.json` 에 휴대폰이 없는지**입니다.

### 숫자는 함부로 바꾸지 마십시오

1회차의 **"참여 학생의 70%가 컴퓨터·AI 전공이 아니었다"** 는 몰입형 99명과 교과형 222명을
합친 기준입니다. 몰입형만 보면 50%입니다. **총인원 숫자는 본문에 넣지 않기로 했습니다** —
"몇 명 중 70%냐"는 질문을 부르기 때문입니다. 근거 데이터는
`~/Downloads/[양성인원] 2026 첨단산업 인재양성 부트캠프.xlsx` 입니다(저장소 밖).

메일에 **다른 교수 이름을 쓰지 않기로** 했습니다. 학과 단위로만 씁니다.

확인 스크립트: `tools/t-episode.mjs` (갈래 배정·조립·자리표시자·발송 기록·원고 편집 24가지).

**표기.** `비` 의 뜻은 `연구년 등으로 제외` 이고, 분석 페이지 범례에서만 인원을 붙여
`연구년 등으로 N명 제외` 로 보여 줍니다.

**선호도 색 (2026-09-21 기준).** 확 `#38831c` · 중 `#0a7ab0` · 모 `#6b7280` · 부 `#c0392b` ·
비 `#7c5cbf` · 미지정 `#b9c0cc`. 막대 안 숫자와 선택된 칩 글자는 모두 흰색입니다.
**색을 바꿀 때는 흰 글자와의 명암 대비를 4.5 이상으로 유지하십시오** — 밝게 잡았다가 숫자가
안 읽혀 두 번 되돌린 적이 있습니다. 바꿔야 할 곳은 `app.js` 의 `RCOLOR` 와 `styles.css` 의
`.rate__b--*` `.prof[data-rating]` `.filters--rate .chip[data-val]` `.rs--*` `.sw--*` `.sbar__seg--*`
여섯 군데이며, 저장 상태 표시·안내 박스·`--accent` 같은 다른 색은 선호도와 무관하니 건드리지 마십시오.
**예외 한 곳:** `.prof[data-rating="모"]` 의 테두리는 일부러 한 단계 연한 `#9ca3af` 입니다.
카드 테두리를 흐리게 두려던 것이므로 `#6b7280` 으로 맞추지 마십시오.
흰 글자가 올라가는 곳(막대 숫자·선택된 칩)은 2026-09-20 기준 대비 4.74~5.44 로 모두 4.5를 넘습니다.
학과 행의 작은 칩 `.rs--*` 은 색 글자를 같은 색 연한 배경에 올리는 구조라 라이트 3.72~4.26,
다크 3.18~3.48 로 모자랐습니다. 2026-09-20 에 **글자색만** `--rs-high/mid/low/neg/na` 변수로 빼고
테마별로 밝기를 옮겨 두 테마 모두 4.55 이상으로 맞췄습니다(색상·채도는 그대로).
**선호도 색 자체는 손대지 않았으니** 막대·테두리·채운 칩은 위에 적은 값 그대로입니다.

대비는 `tools/contrast.mjs` 로 잽니다(`node contrast.mjs [url]`). **색 문자열을 숫자로 긁지 말고
캔버스에 합성해서 픽셀을 읽어야 합니다** — `color-mix` 를 브라우저가 `color(srgb 0~1)` 로 돌려주기
때문에 숫자만 뽑으면 255배 틀린 값이 나옵니다(한 번 이 때문에 잘못된 수치를 보고한 적이 있습니다).

**아이콘.** 좌상단·로그인 화면 마크는 "사람 + 체크 배지" SVG 이고, 탭 아이콘과
`apple-touch-icon.png`(180×180)도 같은 모양입니다. iOS 는 SVG favicon 을 홈 화면에 쓰지 않으므로
PNG 가 반드시 필요합니다.

## 7. 사용자와 일하는 방식

- 한국어로 답합니다.
- 파일을 **삭제하기 전에는 반드시 확인**을 받습니다.
- 모르는 사실은 채우지 말고 보고합니다. 임용 시기는 현재 70명 중 약 31명만 확인되었습니다.
  나머지는 교무처 명부가 있어야 채울 수 있습니다.
- 제주대 학과 홈페이지는 외부에서 직접 가져오지 못하는 경우가 많습니다(robots/SSL).
  각 학과 `professorinfo.htm` 의 교수 상세(+ 버튼) 약력에 임용 연도가 들어 있습니다.

## 8. 알려진 함정

- **CSS 규칙을 통째로 바꿀 때 블록 끝을 정확히 잡으십시오.** 떠 있는 `}` 하나가 남으면 그 아래
  규칙이 통째로 무시됩니다(`.legend` 가 이 때문에 죽어 범례가 붙어 보였습니다).
- **같은 선택자가 뒤에 또 있으면 뒤가 이깁니다.** `.rs--fav` 를 기본 `.rs` 보다 앞에 두어
  배경색이 계속 덮였던 적이 있습니다. 새 변형 규칙은 반드시 기본 규칙 **뒤**에 두십시오.
- **HTML 속성 안의 `>` 에 주의.** favicon 처럼 SVG 데이터가 들어간 태그를 문자열로 자를 때
  첫 `>` 를 태그 끝으로 착각하면 찌꺼기가 화면에 글자로 나옵니다.
- **폰의 필터 줄은 가로 스크롤 영역입니다.** `.filters` 에 `justify-content:flex-end` 를 주면
  앞쪽 칩이 스크롤로 닿을 수 없는 왼쪽 바깥으로 밀려납니다.
- **화면을 고치면 배포 전에 반드시 렌더링해서 눈으로 확인하십시오.** 이 저장소의 기본 검증
  방법이며, 도구는 `tools/shots.mjs` 입니다.

  ```
  cd tools && npm i && npx playwright install chromium   # 처음 한 번만
  node shots.mjs                                          # 배포본
  node shots.mjs http://localhost:8080/ shots local       # 로컬
  ```

  첫 화면 / 학과(교수 카드) / 분석 × 390px·PC × 라이트·다크 12장을 `tools/shots/` 에 남깁니다.
  이 폴더와 `tools/node_modules` 는 `.gitignore` 로 빠져 있습니다.
  테스트 페이지는 `body class="authed"` 가 있어야 하므로(로그인 전에는 `main` 이
  `visibility:hidden`) 스크립트가 `enterApp(null)` 로 게이트를 건너뜁니다. 선호도·관심 교수는
  브라우저 메모리에만 채우며 `favSave()` 를 부르지 않으므로 **시트로 나가는 요청이 없습니다.**

- 시트 401 폴백 메시지는 정상입니다.
- `WebFetch` 는 URL별로 15분간 응답을 캐시합니다. 배포 확인은 반드시 쿼리스트링을 바꿔서 하십시오.
- 예전에 원격 셸이 `.git/index.lock` 을 남겨 커밋이 막힌 적이 있습니다. Claude Code는 사용자
  컴퓨터에서 직접 돌므로 이 문제가 없습니다. 그래도 lock 파일이 보이면 삭제 후 진행하십시오.
